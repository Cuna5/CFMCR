# CFM 遥感去云：厚云区域细节恢复改进方案（评审修订版）

## 1. 问题定义与总体判断

当前问题可以描述为：**单步 CFM 在厚云区域容易产生低频正确但高频不足的结果，表现为纹理平滑、边界模糊和局部结构不稳定。**

需要谨慎的是，这一现象不能直接归因于“缺少随机扰动导致均值坍缩”。更可能的原因包括：

- 厚云区域存在一对多的不适定映射，像素级回归损失倾向于预测条件均值；
- 单步推理要求网络一次完成大幅度映射，起点附近的训练覆盖可能不足；
- 当前辅助条件经过全局池化后只参与通道调制，空间位置信息利用有限；
- 训练目标主要服务 PSNR、SSIM 和 RMSE，与视觉锐度并不完全一致；
- 数据中的云掩膜、厚云样本比例和复杂纹理区域采样可能不够准确或均衡。

因此，建议先建立可验证的诊断基线，再按风险从低到高推进。下文保留损失函数、推理策略、空间架构、条件输入和训练分布五个方向，但不把任何单项改动表述为必然有效。

---

## 2. 当前仓库基础

当前实现已经具备以下能力：

| 模块 | 当前状态 | 说明 |
| --- | --- | --- |
| CFM 监督 | 已实现 | 包含端点一致性、速度一致性、速度锚点和 clean endpoint 监督 |
| 重建损失 | 已实现 | 当前使用 Charbonnier，而不是纯 \(L_2\) |
| 云区加权 | 已实现 | `cloud_loss_weight=2.0`，并对速度锚点启用加权 |
| 非云区保持 | 已实现 | `non_cloud_identity_loss_weight=0.2` |
| 结构损失 | 已实现但默认关闭 | `ssim_endpoint_loss_weight=0.0` |
| 起点过采样 | 已实现 | `start_pair_prob=0.35`，强化一步推理对应的 \(\sigma\approx1\) 区域 |
| NDVI/边缘辅助条件 | 已实现 | `aux_cond` 为 5 通道：NDVI、RGB 边缘和 NIR 边缘 |
| 时间调制辅助编码 | 已实现 | `aux_global_mode="tmm"` |
| 当前边缘输入 | 实际关闭 | 配置使用 `aux_mode="ndvi_only"`，四个边缘通道被置零 |

这意味着后续工作不应从“重新拼接边缘通道”开始，而应先验证现有 `aux_cond` 的有效性。还需注意：当前辅助特征最终经过全局池化后注入时间条件，因此它能调节整体生成过程，但**不能直接提供像素级的空间约束**。

---

## 3. 优先级 P0：建立分层诊断基线

在修改模型前，先确认模糊究竟集中在哪些区域，否则容易把视觉感受误判为整体退化。

建议至少报告：

- 全图 PSNR、SSIM、RMSE；
- 云区与非云区的分区指标；
- 按云覆盖率或云厚度分组的指标；
- 边缘区域的梯度误差或 edge-PSNR；
- 一步与三步 CFM 的对照；
- 高频能量谱或拉普拉斯响应，仅作为锐度诊断，不作为唯一质量指标。

同时应检查当前掩膜：

```python
M = np.clip((t - x).sum(axis=2), 0, 1)
```

该掩膜由真值图像生成，可用于训练加权和评估，但不能作为推理条件。它也可能受到通道差值抵消和辐射差异影响，未必等价于真实云掩膜。建议可视化抽查，并与绝对差、阈值化或已有云掩膜进行对照。

---

## 4. 优先级 P1：验证现有边缘条件并增强空间注入

### 4.1 先做零成本消融

当前代码已经支持 Sobel 边缘，只需比较：

```text
A0: use_aux_cond = false
A1: aux_mode = ndvi_only
A2: aux_mode = full
```

这一实验可以回答：含云 RGB/NIR 中可观测的边缘是否真的有帮助。

需要明确其能力边界：

- 光学 RGB 和 NIR 不能穿透厚云，不能从完全遮挡区域提取真实地表骨架；
- 从含云影像提取的边缘可能主要是云边界，甚至会引入错误引导；
- NIR 对薄云、植被和水体边界可能有帮助，但不能笼统表述为“穿透厚云”；
- SAR 只有在数据集提供配准雷达影像时才可使用，当前 CUHK 的 RGBNIR 输入不能直接替代 SAR。

因此，建议优先使用 Sobel/Scharr 等连续梯度，并对各通道做稳健归一化。Canny 的阈值敏感性较高，不宜直接作为默认方案。

### 4.2 从全局调制升级为空间条件注入

如果 `A2` 优于 `A1`，下一步应保留 `in_channels=8`，将辅助条件编码成多尺度空间特征，在高分辨率层注入，而不是简单修改主输入通道数：

```text
aux_cond
  -> AuxEncoder
  -> {F1, F2, F3}
  -> zero-init 1x1 projection
  -> 对应尺度的 down/up feature
```

可采用：

$$
h_l' = h_l + \gamma_l(t)\,P_l(F_l),
$$

其中 \(P_l\) 使用零初始化，\(\gamma_l(t)\) 为可选的时间门控。这样既能保留旧 checkpoint 的初始行为，也能让边缘和 NDVI 真正携带空间位置信息。

---

## 5. 优先级 P1：局部高频残差适配器

HDiT 已在高分辨率层使用 neighborhood attention，并非完全缺少局部建模。因此不建议立即重写所有 Transformer Block，而是在一至两个高分辨率层加入轻量局部适配器：

```python
residual = local_adapter(x)
x = transformer_block(x, pos, cond)
x = x + residual
```

`local_adapter` 可使用深度可分离 \(3\times3\) 卷积、门控卷积或小型残差块，最后一层采用零初始化。重点验证：

- 是否改善云区边缘而不破坏非云区；
- 是否只是提高锐度，还是确实提升结构一致性；
- 参数量、训练耗时和一步推理延迟的变化。

局部适配器能够增强局部连续性，但不能凭空恢复被厚云完全遮挡的真实纹理。论文中应表述为“增强局部归纳偏置和高频建模能力”，避免使用“保证恢复真实结构”等过强结论。

---

## 6. 优先级 P1：训练时间分布优化

当前实现不是直接使用 `t = torch.rand(...)`，而是在离散 \(\sigma\) 轨迹上采样相邻时间对，并以 `start_pair_prob=0.35` 额外采样 \(\sigma\approx1\) 的起点区域。

因此，直接替换为 \(\mathrm{Beta}(2,2)\) 并不合适：

- 它会减少起点区域采样，可能与一步推理目标冲突；
- “中间阶段最困难”目前只是待验证假设；
- \(\mathrm{Beta}(2,2)\) 在 \([0.3,0.7]\) 内的概率约为 56.8%，并非“超过 60%”；
- 非均匀采样会改变训练目标的隐式权重；若希望保持原目标无偏，需要使用重要性权重修正。

建议优先扫描：

```text
start_pair_prob: 0.35 / 0.50 / 0.70
num_steps:       20 / 40 / 80
```

也可以采用“起点 + 均匀轨迹”的混合采样，而不是只强调中间时间段。若分层诊断表明误差确实集中在中段，再增加中段采样，并单独做消融。

---

## 7. 优先级 P2：结构与高频损失

当前主损失已经是 Charbonnier，因此“从 \(L_2\) 改为鲁棒损失”不再是待办项。低风险尝试应优先使用已有的 MS-SSIM 端点损失：

```text
ssim_endpoint_loss_weight: 0.02 / 0.05 / 0.10
```

还可在云区增加小权重梯度一致性：

$$
L_{\mathrm{grad}}
=
\left\|M\odot\left(\nabla \hat{x}-\nabla x_{\mathrm{clean}}\right)\right\|_1.
$$

总损失可写为：

$$
L_{\mathrm{total}}
=
L_{\mathrm{CFM}}
+\lambda_{\mathrm{ssim}}L_{\mathrm{MS\text{-}SSIM}}
+\lambda_{\mathrm{grad}}L_{\mathrm{grad}}.
$$

结构损失权重过大会牺牲 PSNR 或产生过锐边缘，必须同时报告全图、云区和非云区指标。

---

## 8. 优先级 P3：对抗微调（高风险备选）

对抗训练可以提高感知锐度，但也会带来遥感任务中特别需要警惕的虚假纹理和结构幻觉。它通常改善视觉观感，却不保证提升 PSNR、SSIM 或光谱保真度。

原方案中的

$$
\log D(x_{\mathrm{real}})+\log(1-D(x_{\mathrm{fake}}))
$$

是判别器目标，不能直接作为生成器要最小化的 \(L_{\mathrm{GAN}}\)。若采用 hinge GAN，可写为：

$$
L_D=
\mathbb{E}[\max(0,1-D(x_{\mathrm{real}}))]
+\mathbb{E}[\max(0,1+D(\hat{x}_{\mathrm{detach}}))],
$$

$$
L_G^{\mathrm{adv}}=-\mathbb{E}[D(\hat{x})],
\qquad
L_G=L_{\mathrm{CFM}}+\lambda_{\mathrm{adv}}L_G^{\mathrm{adv}}.
$$

工程建议：

- 仅在稳定 CFM checkpoint 上进行短周期、小学习率微调；
- 使用条件 PatchGAN，使判别器同时观察含云输入和恢复结果；
- 保留重建、光谱和非云区一致性约束；
- 监控云区指标、非云区失真和伪结构，而不仅是视觉锐度；
- 若任务以定量重建为主，对抗训练应保持为可选分支。

---

## 9. 优先级 P3：CFG（不建议作为首选）

CFG 不是“纯推理端、训练无开销”的改动。模型必须在训练时见过无条件分支，推理时还需要两次网络前向。

本任务中的条件图 \(\mu\) 同时承担两种角色：

1. CFM 轨迹的起点；
2. 网络的观测条件。

直接把 `cond_image` 置零，会使“轨迹仍从 \(\mu\) 出发，但条件变成零图”，形成明显的训练分布错配。并且当前 `VanillaCFG` 只对部分条件键做拼接，`aux_cond` 也需要同步适配，不能只改一行 DataLoader。

如果仍要实验，较稳妥的定义是：

- 始终保留轨迹起点 \(\mu\)；
- 只随机丢弃 NDVI/Edge 等辅助条件，或增加显式的条件存在标记；
- 训练和推理使用完全一致的无条件定义；
- 从较小引导系数开始，如 \(s=1.0,1.2,1.5,2.0\)；
- 检查过饱和、边缘振铃、非云区偏移和光谱失真。

CFG 更适合具有丰富语义条件的生成任务。对于以输入影像为强约束的确定性恢复任务，其收益并不确定，优先级应低于空间条件注入和训练分布优化。

---

## 10. 推荐实施路线

### 阶段 A：定位问题

1. 固定当前一步 CFM 基线。
2. 增加云区、非云区、厚云分组和边缘指标。
3. 对比一步与三步结果，判断瓶颈主要来自模型容量还是一步映射。
4. 抽查 `M` 掩膜质量。

### 阶段 B：低风险消融

1. 比较 `use_aux_cond=false`、`ndvi_only` 和 `full`。
2. 扫描 `start_pair_prob`。
3. 小权重启用 MS-SSIM；必要时加入梯度损失。

### 阶段 C：架构增强

1. 将全局 `aux_cond` 扩展为多尺度空间注入。
2. 在高分辨率层加入零初始化局部适配器。
3. 分别消融空间条件、局部适配器及二者组合。

### 阶段 D：高风险探索

只有在阶段 B、C 仍无法满足视觉质量要求时，再评估对抗微调。CFG 可作为探索性实验，不建议直接纳入主方法。

---

## 11. 最小消融矩阵

| 实验 | 边缘条件 | 空间注入 | 局部适配器 | 结构损失 | 对抗损失 |
| --- | --- | --- | --- | --- | --- |
| B0 | 否 | 否 | 否 | 否 | 否 |
| B1 | NDVI | 全局 | 否 | 否 | 否 |
| B2 | NDVI + Edge | 全局 | 否 | 否 | 否 |
| B3 | NDVI + Edge | 多尺度 | 否 | 否 | 否 |
| B4 | NDVI + Edge | 多尺度 | 是 | 否 | 否 |
| B5 | NDVI + Edge | 多尺度 | 是 | 是 | 否 |
| B6 | NDVI + Edge | 多尺度 | 是 | 是 | 是 |

以 B0/B1/B2 验证先验是否有效，以 B3/B4 验证架构贡献，以 B5/B6 判断结构损失和对抗训练是否值得承担失真风险。

---

## 12. 可用于论文的审慎表述

> 针对单步一致性流匹配在厚云区域容易出现高频细节不足的问题，我们从条件信息的空间利用和局部结构建模两个方面增强网络。方法首先从推理阶段可获得的 RGBNIR 观测中提取 NDVI 与多波段梯度特征，再通过时间调制的多尺度条件编码器将其注入主干网络；同时在高分辨率层引入零初始化的局部残差适配器，以增强边界连续性和高频建模能力。所有模块均在不改变 CFM 直线轨迹及一步采样形式的前提下实现，并通过分区指标和逐项消融评估其有效性。

该表述与当前代码结构更一致，也避免把无法从厚云观测中恢复的内容描述为确定性“真实纹理重建”。
